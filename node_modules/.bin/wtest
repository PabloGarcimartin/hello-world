#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../wTesting/proto/dwtools/atop/tester/Exec" "$@"
  ret=$?
else 
  node  "$basedir/../wTesting/proto/dwtools/atop/tester/Exec" "$@"
  ret=$?
fi
exit $ret
